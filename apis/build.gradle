plugins {
    id 'java'
}

group 'de.hhn.it.pp'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    // see also the dependencies in build.gradle (root)
}

test {
    useJUnitPlatform()
}
